function startTimer(duration) {
    setTimeout(function() {
      alert('Timer finished!');
    }, duration);
  }
  
  // Usage example: Start a timer for 5 seconds (5000 milliseconds)
  startTimer(5000);
  